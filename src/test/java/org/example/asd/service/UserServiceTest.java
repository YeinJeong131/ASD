package org.example.asd.service;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class SmokeTest {

    @Test
    void itRuns() {
        // Minimal assertion so the test suite verifies JUnit is working
        assertEquals(2, 1 + 1);
    }
}
